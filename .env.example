# Claude API Key - Required for generating JSON sections
# Get your key at https://console.anthropic.com/
ANTHROPIC_KEY=your_api_key_here

# Custom path to XeLaTeX (optional)
# Only needed if LaTeX is installed in a non-standard location
# XELATEX_PATH=/path/to/your/xelatex
